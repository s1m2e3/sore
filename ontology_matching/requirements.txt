# Ontology matching pipeline dependencies
#
# ALWAYS activate the virtual environment first:
#   Windows:  .venv\Scripts\activate
#   Linux:    source .venv/bin/activate
#
# Install with:
#   pip install -r requirements.txt
#
# PyTorch with CUDA 12.6 (GPU-accelerated, recommended):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# For CPU-only (slower, ~10x):
#   pip install torch torchvision torchaudio

# Core
numpy==2.4.4
scipy>=1.17
owlready2>=0.50

# NLI / transformer model
transformers==5.4.0
# PyTorch — install the CUDA build using the command above; do NOT use plain pip install torch
torch==2.11.0+cu126
torchvision==0.26.0+cu126
torchaudio==2.11.0+cu126

# Synonym matching (Stage 8) + Matryoshka characterizer (Stage 3)
sentence-transformers>=3.0

# Stage 3: WordNet anchor enrichment + covariance estimation + nomic model dep
nltk>=3.9
scikit-learn>=1.4
einops>=0.8

# Visualization
matplotlib>=3.9
networkx>=3.4
plotly>=5.24

# AML runner
# AgreementMakerLight v3.2 JAR — place at enriched_ontology_matching/AML/AML.jar
# Requires Java 11+: https://github.com/AgreementMakerLight/AML-Project

# LogMap runner
# LogMap 4.0 standalone JAR — place at enriched_ontology_matching/logmap/logmap-matcher-4.0-standalone.jar
# Requires Java 11+: https://github.com/ernestojimenezruiz/logmap-matcher/releases

# Enriched ontology matching pipeline (enriched_ontology_matching/)
# OWL generation for AML/LogMap input
rdflib>=6.3

# NLTK data (run once after install):
#   import nltk
#   nltk.download('wordnet')
#   nltk.download('omw-1.4')
#   nltk.download('wordnet_ic')   # required for Lin-IC Layer 3

# ConceptNet (choose one):
#   Option A — API: no install needed, queries https://api.conceptnet.io (rate-limited)
#   Option B — Local CSV: download assertions CSV (~1.5 GB) from
#              https://github.com/commonsense/conceptnet5/wiki/Downloads
#              and place at:
#              ontology_matching/inputs/conceptnet-assertions-5.7.0.csv/assertions.csv
